/* Remember that blanket will only work with brackets live preview */
/* Try to maximise the coverage of the ParlaModel object */

describe("Trivia", function() {

  it("tell you if your anwers are correct", function() {  
	  expect(isAnswerCorrect("In which year were the first Modern Olympics held?")).toBe("1896")
      it(" should tells you if there are no more questions")
  });    , function { expect(next()).not.toBe(null);
	  
  }
 
});
